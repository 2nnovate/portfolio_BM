import React, { Component } from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};
const defaultProps = {
};

class Call extends Component {
    state = {

    }
    render() {
        return(
            <div>
              전화 주문의 목록 뷰
            </div>
        );
    }
}

Call.propTypes = propTypes;
Call.defaultProps = defaultProps;

export default Call;
